[package]
name = "filesearch"
version = "0.1.0"
edition = "2024"

[dependencies]
walkdir = "2.5"
ignore = "0.4"
sysinfo ="0.30"

[package.metadata.packager]
product_name = "searchfile"
version = "0.1.0"
description = "This app searches for a file in your whole PC in mere milliseconds"
authors = ["IRISHYURA Yedidiah"]
icons = ["filesearch.png"]

[package.metadata.packager.nsis]
installer_mode = "both"
